<nav  id="nav_admin" class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#"><img id="monlog" src="{{ asset('/Images/log.png') }}"/>TrocLivres</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Categories 
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            {% for categorie in categories %}
                                <a class="dropdown-item" href="{{ path('categorie_livres', {id: categorie.id}) }}">{{categorie.nomCategorie}}</a>
                            {% endfor %}
                        </div>
                    </li>
                
                
                
             
                    <form class="form-inline" method="post" action="{{ path('moteur_recherche') }}">
                       <li class="nav-item">
                        <input name ="text"  class="form-control mr-sm-2" type="search" placeholder="Rechercher un livre ou auteur" aria-label="Search" required/>
                       </li>
                       <li class="nav-item">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Rechercher</button></li>
                    </form>
               
                 {% if app.user and  is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a id='aaddmin' class="nav-link" href="{{ path('admin') }}">Espace Admin</a>
                    </li>
                {% endif %}
                </ul>
            </div>
        </nav>